services:
  web:
    build: .
    restart: on-failure
    ports:
      - "8080:5000"
    environment:
      - REDIS_HOST=redis
    depends_on:
      redis:
        condition: service_started

  redis:
    image: redis:8-alpine
    restart: on-failure
